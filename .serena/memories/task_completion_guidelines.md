当任务完成时，建议执行以下步骤以确保代码质量：

*   **测试：** 运行 `pytest` 命令来执行测试套件，确保所有功能按预期工作。在 `tests/` 目录中寻找更多测试相关的配置或脚本。
*   **格式化：** 使用 `black .` 或 `autopep8 .` 等工具对代码进行格式化，以确保代码风格一致性。根据项目实际配置，选择合适的格式化工具。
*   **Linting：** 运行 `flake8 .` 或类似命令执行 Linting 检查，以识别潜在的代码质量问题和风格不一致。在项目文档或配置中查找具体的 Linting 规则。